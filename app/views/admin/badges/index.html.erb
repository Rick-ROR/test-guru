<div class="p-3 bg-gradient-light text-dark">
  <h1><%= t('.header') %>:</h1>
</div>

<p>
  <%= link_to t('create'), new_admin_badge_path, class: "btn btn-outline-success" %>
</p>

<table class="table">
  <thead class="thead-light">
    <tr>
      <th></th>
      <th><%= t('title') %></th>
      <th><%= t('.rule') %></th>
      <th><%= t('.sub_rule') %></th>
      <th><%= t('date') %></th>
      <th colspan="3"><%= t('.action') %></th>
    </tr>
  </thead>

  <%= render @badges %>
</table>
