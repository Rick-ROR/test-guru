<% if user_signed_in? %>
  <p class="nav user">
    Привет, <b><%= current_user.name %></b> ! <%= link_to '#Guru', tests_path %>
  </p>
  <p class="nav user">
    <%= link_to 'Выйти', destroy_user_session_path, method: :delete %>
  </p>
<% elsif current_page?(new_user_registration_path) %>
  <p class="nav user"><%= link_to 'Войти', new_user_session_path %></p>
<% else %>
  <p class="nav user"><%= link_to 'Регистрация тут', new_user_registration_path %></p>
<% end %>
